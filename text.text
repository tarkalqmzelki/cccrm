Analyze the project and index all components. We need to perform a tweak before doing anything. So in the left corner bottom where is the dropdown (up) menu, when clicken on given access or, inbox it gives 404. Based on the current program, we need to integrate the messaging system in the inbox. The inbox have the request access notifications currently, but we need to make in inbox a button called send message (which sends an email-like message to another member of the platform), furthermore, we must have also Start chat, which should trigger the general chat in the platform, where all people can generally chat. Use the current database structures. 